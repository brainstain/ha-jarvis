{
  "config": {
    "step": {
      "user": {
        "title": "Connect to Agent Orchestrator",
        "description": "Enter the base URL of your agent-orchestrator instance (e.g., http://192.168.13.22:8100).",
        "data": {
          "base_url": "Base URL",
          "api_key": "Bearer Token (optional)"
        }
      }
    },
    "error": {
      "cannot_connect": "Cannot connect to agent-orchestrator. Ensure it is running and the URL is correct (checks GET {base_url}/ha/health)."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "HA Jarvis Options",
        "description": "Configure the conversation agent behavior.",
        "data": {
          "try_ha_first": "Try Home Assistant intents first (device control)"
        }
      }
    }
  }
}
